 Input: Taking a positive integer n from the user
n = int(input("Enter a positive integer: "))

Initialize a variable to store the sum of cubes
sum_of_cubes = 0

Loop through all even numbers less than or equal to n
for i in range(2, n + 1, 2):
    sum_of_cubes += i ** 3  # Add the cube of the even number to the sum

 Output the
